specVersion: "v1beta"
serviceId: "asal firebase"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "asal firebase-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
